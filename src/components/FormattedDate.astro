---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-white/30 block">
	{
		date.toLocaleDateString('tr-Tr', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
